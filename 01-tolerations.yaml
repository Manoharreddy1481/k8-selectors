apiVersion: v1
kind: Pod
metadata:
  name: nginx
  namespace: expense
spec:
  containers:
  - name: nginx
    image: nginx
  tolerations:
  - key: "hardware"
    operator: "equal"
    value: "gpu"
    effect: "NoSchedule"
